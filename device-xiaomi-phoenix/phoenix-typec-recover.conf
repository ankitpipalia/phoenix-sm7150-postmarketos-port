# phoenix-typec-recover config -- capacity threshold below which the
# stuck-as-source rescue is allowed to run.
#
# The rescue causes a brief (~3-5s) eth0 hiccup if no external power is
# actually available, so the default threshold sits well below the
# charge-cap band to ensure normal operation never hits it.
CAP_THRESHOLD=30
