# Phoenix battery telemetry configuration.
# Five-second samples are detailed enough for charge/capacity integration while
# remaining small enough for continuous server use (roughly a few MiB per day).
INTERVAL_SECONDS=5
LOG_DIR=/var/log/phoenix-battery
RETENTION_DAYS=14
