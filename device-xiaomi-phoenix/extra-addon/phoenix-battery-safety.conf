# Conservative starting values for the Phoenix replacement battery.
# Validate these values with a controlled discharge before production use.
INTERVAL_SECONDS=5
SHUTDOWN_VOLTAGE_UV=3450000
SHUTDOWN_SAMPLES=12
EMERGENCY_VOLTAGE_UV=3350000
EMERGENCY_SAMPLES=3
MAX_TEMP_DECIC=450
MAX_TEMP_SAMPLES=6
SHUTDOWN_COMMAND="systemctl poweroff"
DRY_RUN=0
